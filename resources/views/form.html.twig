{% extends "layout.html.twig" %}

{% block title "Page form" %}

{% block content %}
    <h1>{{ 'Form'|trans }}</h1>

    <h2>{{ content.form.title }}</h2>

    {{ form(form, {action: path('form')}) }}
{% endblock %}